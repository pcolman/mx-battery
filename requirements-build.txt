# Build-time dependencies for packaging the .app bundle. All versions pinned.
py2app==0.28.8
altgraph==0.17.5
macholib==1.16.4
modulegraph==0.19.7
packaging==26.2
# py2app 0.28.8 requires pkg_resources, removed in newer setuptools.
setuptools==83.0.0
# pkg_resources (imported by py2app's boot script) needs jaraco at runtime,
# so these must be installed for the bundle to include them.
jaraco.text==4.3.0
jaraco.context==6.1.2
jaraco.functools==4.6.0
more_itertools==11.1.0
